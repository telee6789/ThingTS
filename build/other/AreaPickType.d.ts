export declare enum AreaPickType {
    /**
     * 默认方式，从左上角触发的框选为实时选择(速度慢), 其他方式为鼠标抬起后选择(速度快)
     */
    Auto = 0,
    /**
     * 实时状态选择
     */
    RealTime = 1,
    /**
     * 非实时状态选择
     */
    NotRealTime = 2
}
